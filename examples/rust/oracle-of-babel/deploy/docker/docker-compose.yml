version: '3.8'

services:
  oracle-of-babel:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - RUST_LOG=info
    restart: unless-stopped
