# 🕵️ AGENT CREDENTIALS (CLASSIFIED)
#
# Enter the private key for your "Burner" wallet based on your target network.
#
# ⚠️ MISSION ADVISORY:
# - SIMULATIONS (Devnet/Testnet): Utilize free assets from headquarters (faucets).
# - LIVE FIRE (Mainnet): Real financial assets will be expended. Proceed with caution.
#
# 🔑 SOLANA (SVM) NETWORKS: solana, solana-mainnet
# Base58 encoded private key
# - Field Agents (Phantom/Solflare): Settings -> Export Private Key
# - Terminal Uplink (CLI): solana-keygen recover
SVM_PRIVATE_KEY=

# 🔑 BASE (EVM) NETWORKS: base, base-mainnet
# Hex encoded private key (with or without 0x prefix)
# - Field Agents (MetaMask): Settings -> Security -> Reveal Private Key
# - Terminal Uplink (CLI): cast wallet new
EVM_PRIVATE_KEY=
